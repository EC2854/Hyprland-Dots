Config(
    x: Fraction(0.5),
    y: Absolute(0),
    width: Absolute(600),
    height: Absolute(0),
    hide_icons: false, 
    ignore_exclusive_zones: true, 
    layer: Overlay, 
    hide_plugin_info: true, 
    close_on_click: true,
    show_results_immediately: false,
    max_entries: None,
    plugins: [
        "libapplications.so",
        "libshell.so",
        "libwebsearch.so",
        "librink.so",
    ],
)
